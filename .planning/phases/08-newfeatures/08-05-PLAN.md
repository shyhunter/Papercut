---
phase: 08-newfeatures
plan: 05
type: execute
wave: 2
depends_on: [08-01, 08-02, 08-03, 08-04]
files_modified: []
autonomous: false
requirements: [SC-1, SC-2, SC-3, SC-4, SC-5, SC-6, SC-7]

must_haves:
  truths:
    - "All 7 success criteria verified visually by the user"
  artifacts: []
  key_links: []
---

<objective>
Visual verification checkpoint for all Phase 8 UX polish features.

Purpose: Confirm all 7 success criteria are met before marking the phase complete.
Output: User approval or issue list for gap closure.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify all Phase 8 UX polish features</name>
  <what-built>
All Phase 8 UX polish features implemented in plans 01-04:
1. Compressibility detection + inline warning + progression block (ConfigureStep)
2. Always-visible compressibility threshold info (ConfigureStep)
3. Custom 5th radio quality option with MB/KB target size input + validation (ConfigureStep)
4. Full stats row above panels + floating zoom toolbar + synced panel scrolling (CompareStep + ImageCompareStep)
5. Persistent save confirmation card with X close + clickable file link + animated checkmark (SaveStep + App.tsx)
6. "Process Another" renamed to "Start Over" (CompareStep + ImageCompareStep)
7. Responsive scaling of all UI elements with window size (App.css + globals.css + components)
  </what-built>
  <how-to-verify>
1. **Compressibility feedback (text-only PDF):**
   - Open a text-only PDF (no embedded images)
   - Verify: amber inline warning appears in Configure step
   - Verify: "Generate Preview" button is disabled (blocked)
   - Enable "Resize pages" toggle — verify button becomes enabled

2. **Compressibility feedback (image-heavy PDF):**
   - Open an image-heavy PDF (e.g., test-fixtures/photo_heavy.pdf or sample.pdf)
   - Verify: positive compressibility guidance is shown (not a warning)
   - Verify: Generate Preview button is enabled

3. **Custom quality option:**
   - In Configure step, verify 5 radio buttons: Web, Screen, Print, Archive, Custom
   - Select "Custom" — verify integer input field with MB/KB toggle appears
   - Enter a value larger than original file — verify error message blocks
   - Enter a valid smaller value — verify Generate Preview works

4. **Compare step layout:**
   - Process a PDF — verify stats row appears ABOVE the comparison panels
   - Verify floating zoom toolbar appears at bottom-center of panels area
   - Zoom in to 150% — scroll one panel — verify other panel scrolls in sync
   - Verify "Start Over" label (not "Process another")
   - Repeat with an image file

5. **Save experience:**
   - Click Save in Compare step
   - After saving, verify a persistent card appears at top with:
     - Animated green checkmark (circle draws in, then check, then glow)
     - "File saved successfully" text
     - Clickable file path link
     - X close button
   - Click the file path — verify file opens in default OS app (Preview, etc.)
   - Click X — verify card dismisses
   - Verify the card does NOT auto-disappear

6. **Responsive scaling:**
   - Resize the Tauri window to minimum size (900x660)
   - Verify all text, buttons, inputs, icons are readable and not clipped
   - Resize to large size (1400x900+)
   - Verify everything scales up proportionally
   - Check all 4 steps: Pick, Configure, Compare, Save
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 criteria pass, or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<verification>
All 7 success criteria from the phase definition are verified:
1. Non-compressible files: inline warning + block
2. Compressibility info: always visible
3. Custom quality: 5th radio + MB/KB + validation
4. Compare layout: stats above + floating zoom + synced scroll
5. Save confirmation: persistent card + file link + X close
6. Label: "Start Over"
7. Responsive scaling: all elements scale with window
</verification>

<success_criteria>
User confirms all 7 phase success criteria are met.
</success_criteria>

<output>
After completion, create `.planning/phases/08-newfeatures/08-05-SUMMARY.md`
</output>
