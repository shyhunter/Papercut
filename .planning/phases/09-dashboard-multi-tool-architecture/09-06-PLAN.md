---
phase: 09-dashboard-multi-tool-architecture
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/tools.ts
  - src/context/ToolContext.tsx
  - src/components/Dashboard.tsx
  - src/components/merge/MergePickStep.tsx
  - src/components/merge/MergeFlow.tsx
  - src/components/split/SplitFlow.tsx
  - src/components/split/SplitPickStep.tsx
  - src/components/rotate/RotateFlow.tsx
  - src/App.tsx
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "Dropping multiple PDF files on the dashboard shows only multi-file tools (merge) in the picker"
    - "Dropping a single file on the dashboard shows all compatible tools (no change from before for single files)"
    - "Selecting Merge after multi-file drop pre-loads all dropped files into MergePickStep"
    - "Dropping a single file and choosing Merge does NOT duplicate the file"
    - "Single-file tools (compress, split, rotate) still work correctly with pendingFiles[0]"
  artifacts:
    - path: "src/types/tools.ts"
      provides: "acceptsMultipleFiles flag on ToolDefinition"
      contains: "acceptsMultipleFiles"
    - path: "src/context/ToolContext.tsx"
      provides: "pendingFiles: string[] state (replacing singular pendingFile)"
      contains: "pendingFiles"
    - path: "src/components/Dashboard.tsx"
      provides: "Multi-file drag-drop with tool filtering by file count"
      contains: "acceptsMultipleFiles"
  key_links:
    - from: "src/components/Dashboard.tsx"
      to: "src/context/ToolContext.tsx"
      via: "setPendingFiles(droppedPaths) before selectTool"
      pattern: "setPendingFiles"
    - from: "src/components/merge/MergeFlow.tsx"
      to: "src/context/ToolContext.tsx"
      via: "reads pendingFiles array and passes to MergePickStep as initialFiles"
      pattern: "pendingFiles"
    - from: "src/components/merge/MergePickStep.tsx"
      to: "addFiles"
      via: "useEffect loads initialFiles array on mount"
      pattern: "useEffect.*initialFiles"
---

<objective>
Fix dashboard multi-file drag-and-drop: support dropping multiple files, filter the tool picker to only show multi-file-compatible tools when >1 file is dropped, and forward all dropped files to MergePickStep. Also fix the useState-as-side-effect misuse in MergePickStep, SplitPickStep, and RotateFlow.

Purpose: Close the UAT gap from test 4 — user cannot currently drag multiple files, and single-file merge drop duplicates the file.
Output: Updated tools.ts, ToolContext.tsx, Dashboard.tsx, MergePickStep.tsx, MergeFlow.tsx, SplitFlow.tsx, SplitPickStep.tsx, RotateFlow.tsx, App.tsx
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/types/tools.ts
@src/context/ToolContext.tsx
@src/components/Dashboard.tsx
@src/components/merge/MergePickStep.tsx
@src/components/merge/MergeFlow.tsx
@src/components/split/SplitFlow.tsx
@src/components/split/SplitPickStep.tsx
@src/components/rotate/RotateFlow.tsx
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add acceptsMultipleFiles to ToolDefinition and migrate pendingFile to pendingFiles array</name>
  <files>
    src/types/tools.ts
    src/context/ToolContext.tsx
  </files>
  <action>
**src/types/tools.ts:**
1. Add `acceptsMultipleFiles?: boolean` to the `ToolDefinition` interface (optional, defaults to false/undefined).
2. In `TOOL_REGISTRY`, set `acceptsMultipleFiles: true` on `merge-pdf` only. All other tools leave it unset (implicitly false).

**src/context/ToolContext.tsx:**
1. Change `pendingFile: string | null` to `pendingFiles: string[]` throughout:
   - State: `const [pendingFiles, setPendingFiles] = useState<string[]>([]);`
   - Interface: `pendingFiles: string[];` and `setPendingFiles: (files: string[]) => void;`
2. Update `goToDashboard` to call `setPendingFiles([])` instead of `setPendingFile(null)`.
3. Update the `value` useMemo to include `pendingFiles` and `setPendingFiles` (remove old `pendingFile`/`setPendingFile`).
4. Update the useMemo dependency array accordingly.
  </action>
  <verify>
Run `npx tsc --noEmit` from the project root. It will show type errors in consumers (Dashboard, flows, App.tsx) — that is expected and will be fixed in Task 2. Verify that tools.ts and ToolContext.tsx themselves have no internal errors by checking the tsc output only references other files.
  </verify>
  <done>
ToolDefinition has acceptsMultipleFiles field. merge-pdf has it set to true. ToolContext exposes pendingFiles: string[] and setPendingFiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Dashboard multi-file drop and all flow consumers to use pendingFiles array</name>
  <files>
    src/components/Dashboard.tsx
    src/components/merge/MergePickStep.tsx
    src/components/merge/MergeFlow.tsx
    src/components/split/SplitFlow.tsx
    src/components/split/SplitPickStep.tsx
    src/components/rotate/RotateFlow.tsx
    src/App.tsx
  </files>
  <action>
**src/components/Dashboard.tsx:**
1. Change `droppedFile: string | null` state to `droppedFiles: string[]` (initial `[]`).
2. In the drag-drop handler (line ~68-76), remove the `paths.length === 1` guard. Instead:
   - Filter paths to only supported files: `const validPaths = paths.filter(p => isSupportedFile(p));`
   - If `validPaths.length === 0`, return early.
   - Get compatible tools using `getCompatibleTools(validPaths[0])` (format detection from first file — all files should be same type for multi-file tools).
   - If `validPaths.length > 1`, further filter compatible tools to only those with `acceptsMultipleFiles: true`: `tools.filter(t => t.acceptsMultipleFiles)`.
   - If `validPaths.length === 1`, show all compatible tools (existing behavior).
   - Set `droppedFiles` to `validPaths` and `compatibleTools` to the filtered list.
3. Update `handleToolSelect`: call `setPendingFiles(droppedFiles)` instead of `setPendingFile(droppedFile)`. Reset `droppedFiles` to `[]`.
4. Update `handleDismissPicker`: reset `droppedFiles` to `[]`.
5. Update the tool picker overlay display:
   - Show the file count: if `droppedFiles.length === 1`, show the file name (existing). If >1, show "{N} files selected".
   - The Escape key handler and overlay structure stay the same, just using `droppedFiles.length > 0` instead of `droppedFile`.

**src/components/merge/MergePickStep.tsx:**
1. Change prop `initialFile?: string | null` to `initialFiles?: string[]`.
2. Replace the `useState(() => { ... })` side-effect hack (line 46-50) with a proper `useEffect`:
   ```tsx
   useEffect(() => {
     if (initialFiles && initialFiles.length > 0) {
       addFiles(initialFiles);
     }
   }, []); // Run once on mount — initialFiles is captured from props at mount time
   ```
   Add `useEffect` to the import from 'react'.
3. Do NOT add `addFiles` or `initialFiles` to the dependency array — this must only run once on mount. Add an eslint-disable comment for exhaustive-deps.

**src/components/merge/MergeFlow.tsx:**
1. Change `pendingFile` references to `pendingFiles`.
2. Consume pending files: `const initialFiles = pendingFiles.length > 0 ? [...pendingFiles] : [];` then `setPendingFiles([]);` if non-empty.
3. Pass `initialFiles={initialFiles}` to `MergePickStep` (replacing `initialFile={initialFile}`).

**src/components/split/SplitFlow.tsx:**
1. Change `pendingFile` to `pendingFiles`.
2. Consume: `const initialFile = pendingFiles.length > 0 ? pendingFiles[0] : null;` then `setPendingFiles([]);`.
3. Pass `initialFile={initialFile}` to SplitPickStep (same prop name, just sourced from array).

**src/components/split/SplitPickStep.tsx:**
1. Replace the `useState(() => { ... })` side-effect hack (line 37-41) with a proper `useEffect`:
   ```tsx
   useEffect(() => {
     if (initialFile) {
       loadFile(initialFile);
     }
   }, []); // eslint-disable-line react-hooks/exhaustive-deps
   ```
   Add `useEffect` to the import from 'react'.

**src/components/rotate/RotateFlow.tsx:**
1. Change `pendingFile` to `pendingFiles`.
2. Consume: `const initialFile = pendingFiles.length > 0 ? pendingFiles[0] : null;` then `setPendingFiles([]);`.
3. Replace the `useState(() => { ... })` side-effect hack (line 62-66) with a proper `useEffect`:
   ```tsx
   useEffect(() => {
     if (initialFile) {
       loadFile(initialFile);
     }
   }, []); // eslint-disable-line react-hooks/exhaustive-deps
   ```
   Note: `initialFile` here is a local const captured before the effect, so it is stable.
   Add `useEffect` to the import from 'react'.

**src/App.tsx (ToolFlow function):**
1. Change `pendingFile` and `setPendingFile` destructuring to `pendingFiles` and `setPendingFiles`.
2. Update the `useEffect` that auto-loads pending files (line ~189-194):
   ```tsx
   useEffect(() => {
     if (pendingFiles.length > 0 && currentStep === 0 && !fileEntry) {
       const file = pendingFiles[0];
       setPendingFiles([]);
       handleFileSelected(file);
     }
   }, [pendingFiles, currentStep, fileEntry, handleFileSelected, setPendingFiles]);
   ```
   This useEffect only handles single-file tools (compress-pdf, compress-image). Merge/Split/Rotate flows consume pendingFiles themselves before this code runs.
  </action>
  <verify>
1. Run `npx tsc --noEmit` — zero errors.
2. Run `npm run lint` — no new lint errors (the eslint-disable comments handle the exhaustive-deps).
3. Run `npm run dev` and verify the app compiles and launches without console errors.
  </verify>
  <done>
- Dropping 1 file on dashboard shows all compatible tools (same as before).
- Dropping 2+ PDF files on dashboard shows only "Merge PDFs" in the tool picker (single-file tools are hidden).
- Selecting Merge after multi-file drop pre-loads all dropped files into MergePickStep file list.
- Dropping 1 file and choosing Merge loads that single file (no duplication).
- Split, Rotate, Compress PDF, and Compress Image still work with pendingFiles[0].
- No useState-as-side-effect hacks remain (all replaced with useEffect).
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors.
2. `npm run lint` passes.
3. Manual test: Drop 1 PDF on dashboard -> tool picker shows all PDF tools (compress, merge, split, rotate).
4. Manual test: Drop 3 PDFs on dashboard -> tool picker shows ONLY "Merge PDFs".
5. Manual test: Drop 3 PDFs -> select Merge -> all 3 appear in MergePickStep file list with thumbnails.
6. Manual test: Drop 1 PDF -> select Merge -> only that 1 file in MergePickStep (not duplicated).
7. Manual test: Drop 1 PDF -> select Split -> file loads in SplitPickStep.
8. Manual test: Drop 1 PDF -> select Compress PDF -> file loads in ConfigureStep.
</verification>

<success_criteria>
Multi-file drag-and-drop works on the dashboard. Tool picker filters by file count. Merge receives all dropped files. Single-file tools receive only the first file. No useState-as-side-effect hacks remain. TypeScript compiles cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/09-dashboard-multi-tool-architecture/09-06-SUMMARY.md`
</output>
