---
phase: 09-dashboard-multi-tool-architecture
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/components/StepBar.tsx
  - src/components/ToolHeader.tsx
  - src/components/Dashboard.tsx
  - src/App.tsx
autonomous: true
requirements:
  - SC-01
  - SC-06
  - SC-07

must_haves:
  truths:
    - "StepBar displays tool-specific step labels (e.g., Merge shows Pick Files → Order → Save, not the compress steps)"
    - "A breadcrumb trail shows 'Dashboard > Tool Name' when inside a tool flow"
    - "Back arrow in breadcrumb returns user to dashboard"
    - "Dropping a file on the Dashboard shows a tool picker overlay with compatible tools for that file type"
    - "Dashboard file drop detects PDF vs image and filters tool cards accordingly"
  artifacts:
    - path: "src/components/StepBar.tsx"
      provides: "Adaptive StepBar accepting dynamic steps array from tool definition"
      contains: "steps"
    - path: "src/components/ToolHeader.tsx"
      provides: "Breadcrumb navigation with back-to-dashboard arrow"
      contains: "goToDashboard"
    - path: "src/components/Dashboard.tsx"
      provides: "File drop zone on dashboard with tool picker overlay"
      contains: "tool-picker"
  key_links:
    - from: "src/components/StepBar.tsx"
      to: "src/types/tools.ts"
      via: "ToolStep[] prop replaces hardcoded STEPS array"
      pattern: "ToolStep"
    - from: "src/components/ToolHeader.tsx"
      to: "src/context/ToolContext.tsx"
      via: "useToolContext().goToDashboard"
      pattern: "goToDashboard"
    - from: "src/context/ToolContext.tsx"
      to: "src/components/merge/MergeFlow.tsx"
      via: "pendingFile state forwarded to tool flows for auto-loading dropped files"
      pattern: "pendingFile"
---

<objective>
Make StepBar adaptive (driven by tool definition steps) and add breadcrumb navigation with back-to-dashboard. Also add file-drop-on-dashboard with tool picker overlay. After this plan, every tool shows its own step labels in the progress bar, and users have clear navigation back to the dashboard.

Purpose: Navigation infrastructure that all tools rely on — breadcrumb and adaptive steps are needed before tool-specific flows make sense.
Output: StepBar renders per-tool steps, breadcrumb shows "Dashboard > Tool Name" with clickable back arrow, file drop on dashboard shows tool picker.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/components/StepBar.tsx
@src/components/Dashboard.tsx
@src/context/ToolContext.tsx
@src/types/tools.ts
@src/App.tsx
@src/hooks/useFileDrop.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make StepBar adaptive with dynamic steps</name>
  <files>
    src/components/StepBar.tsx
  </files>
  <action>
Refactor StepBar to accept a `steps` prop instead of using the hardcoded `STEPS` array:

1. Change props from `{ current: AppStep }` to `{ steps: ToolStep[]; current: number }` — import `ToolStep` from `types/tools`
2. Remove the hardcoded `STEPS` constant
3. Render the dynamic `steps` array with the same visual treatment (numbered circles, connector lines, checkmarks for completed steps)
4. The `current` prop is now a 0-based index into the `steps` array (no longer tied to `AppStep` type specifically)

The visual rendering logic (circles, connectors, colors, checkmarks) stays identical — only the data source changes.
  </action>
  <verify>
`npx tsc --noEmit` passes. StepBar now accepts dynamic steps and current index.
  </verify>
  <done>
StepBar renders any tool's step sequence from its ToolDefinition.steps array. No more hardcoded 4-step assumption.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ToolHeader with breadcrumb navigation</name>
  <files>
    src/components/ToolHeader.tsx
    src/App.tsx
  </files>
  <action>
**src/components/ToolHeader.tsx** — New component combining breadcrumb + StepBar:

1. Renders a row with:
   - Left: Back arrow button (← icon from Lucide `ArrowLeft`) + breadcrumb text: "Dashboard > {tool.name}"
   - "Dashboard" is a clickable link that calls `goToDashboard()`
   - Tool name is static text (current location)
2. Below the breadcrumb: the adaptive `<StepBar steps={tool.steps} current={currentStep} />`
3. Style: `px-4 py-2` for breadcrumb row, muted text, hover on "Dashboard" link

**src/App.tsx** — Integration:

1. When `activeTool !== null`: render `<ToolHeader />` instead of the old `<StepBar current={currentStep} />`
2. When `activeTool === null` (dashboard): render NO header bar (dashboard has its own header)
3. Pass `currentStep` to ToolHeader (it forwards to StepBar)
4. Wire `goToDashboard` to reset all state (fileEntry, processors, step) then call `toolContext.goToDashboard()`
  </action>
  <verify>
1. `npx tsc --noEmit` passes
2. Enter Compress PDF — breadcrumb shows "Dashboard > Compress PDF" with back arrow
3. Click "Dashboard" in breadcrumb — returns to dashboard, state is reset
4. StepBar shows correct steps for the active tool
  </verify>
  <done>
Breadcrumb navigation with "Dashboard > Tool Name" renders above StepBar for all tool flows. Back arrow returns to dashboard. StepBar shows tool-specific steps.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add file drop on Dashboard with tool picker</name>
  <files>
    src/components/Dashboard.tsx
    src/hooks/useFileDrop.ts
  </files>
  <action>
**Dashboard file drop behavior:**

1. Dashboard component uses `useFileDrop` (or a simpler Tauri `onDragDropEvent` listener) to detect file drops
2. On drop: detect file type using `detectFormat()` from `fileValidation.ts`
3. Show a tool picker overlay/modal: "What do you want to do with this PDF?" (or image)
4. Filter tools from TOOL_REGISTRY by `acceptsFormats` matching the dropped file type
5. Show matching tools as a list of clickable buttons in a centered overlay (shadcn Card or Dialog style)
6. Clicking a tool: call `selectTool(toolId)` and pass the dropped file path to the tool flow
7. Clicking outside or pressing Escape dismisses the overlay

**Implementation notes:**
- The tool picker is a local state overlay within Dashboard (`droppedFile`, `showPicker`)
- Use the existing `onDragDropEvent` pattern from `useFileDrop.ts` for drag state styling
- After tool selection, the file path needs to be forwarded to the tool flow — store it in ToolContext as `pendingFile?: string` so the tool's LandingCard/Pick step can auto-load it
- Add `pendingFile` and `setPendingFile` to ToolContext interface

**Visual treatment:**
- Dashboard shows drag overlay ("Drop to choose a tool") during drag-over
- Tool picker overlay: semi-transparent backdrop, centered card with tool list
- Each tool button shows icon + name, same style as dashboard cards but in a compact list
  </action>
  <verify>
1. `npx tsc --noEmit` passes
2. Drag a PDF onto the dashboard — shows "Drop to choose a tool" overlay
3. Drop the PDF — tool picker shows PDF-compatible tools (Compress, Merge, Split, Rotate)
4. Drag an image — tool picker shows image-compatible tools (Compress Image)
5. Select a tool from picker — enters that tool's flow
6. Dismiss picker (Escape/click outside) — returns to normal dashboard
  </verify>
  <done>
File drop on Dashboard detects file type, shows tool picker overlay with compatible tools, and selecting a tool enters its flow with the dropped file pre-loaded.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run test` — existing tests still pass
3. StepBar shows tool-specific step labels for each tool
4. Breadcrumb "Dashboard > Tool Name" with back arrow renders for all tool flows
5. File drop on dashboard shows tool picker with correct tools for file type
6. Navigation: dashboard → tool → back to dashboard works cleanly
</verification>

<success_criteria>
- StepBar is fully adaptive — renders steps from tool definition
- Breadcrumb navigation with back-to-dashboard works from any tool
- File drop on dashboard shows tool picker overlay filtered by file type
- Existing compress flows still work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/09-dashboard-multi-tool-architecture/09-02-SUMMARY.md`
</output>
