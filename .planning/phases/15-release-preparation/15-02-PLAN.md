---
phase: 15-release-preparation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/tauri.conf.json
  - package.json
  - src-tauri/Cargo.toml
  - src-tauri/icons/icon.png
  - src-tauri/icons/32x32.png
  - src-tauri/icons/128x128.png
  - src-tauri/icons/128x128@2x.png
  - src-tauri/icons/icon.icns
  - src-tauri/icons/icon.ico
  - src-tauri/icons/Square30x30Logo.png
  - src-tauri/icons/Square44x44Logo.png
  - src-tauri/icons/Square71x71Logo.png
  - src-tauri/icons/Square89x89Logo.png
  - src-tauri/icons/Square107x107Logo.png
  - src-tauri/icons/Square142x142Logo.png
  - src-tauri/icons/Square150x150Logo.png
  - src-tauri/icons/Square284x284Logo.png
  - src-tauri/icons/Square310x310Logo.png
  - src-tauri/icons/StoreLogo.png
  - src/components/SplashScreen.tsx
  - src/App.tsx
  - public/splashscreen.html
autonomous: false
requirements:
  - BRANDING

must_haves:
  truths:
    - "App version reads 1.0.0-beta.1 in all three config files"
    - "App icon shows a scissors/paper themed design in the dock/taskbar"
    - "App shows a brief splash screen with logo on launch before main content appears"
  artifacts:
    - path: "src-tauri/icons/icon.png"
      provides: "Custom app icon source file (1024x1024)"
    - path: "src/components/SplashScreen.tsx"
      provides: "React splash overlay component"
      min_lines: 20
  key_links:
    - from: "src/App.tsx"
      to: "src/components/SplashScreen.tsx"
      via: "conditional render with setTimeout fade"
      pattern: "SplashScreen"
---

<objective>
Set up branding: bump version to 1.0.0-beta.1, create custom app icon, and add splash screen.

Purpose: Transform the app from a development project to a branded beta product. Version signals "this is v1, needs testing." Icon gives professional identity. Splash screen provides polished launch experience.

Output: Version bumped, custom icon in all sizes, splash screen on launch.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src-tauri/tauri.conf.json
@package.json
@src-tauri/Cargo.toml
@.planning/phases/15-release-preparation/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Version bump and app icon creation</name>
  <files>
    src-tauri/tauri.conf.json
    package.json
    src-tauri/Cargo.toml
    src-tauri/icons/ (all icon files)
  </files>
  <action>
    1. Bump version to `1.0.0-beta.1` in all three files:
       - `src-tauri/tauri.conf.json`: `"version": "1.0.0-beta.1"`
       - `package.json`: `"version": "1.0.0-beta.1"`
       - `src-tauri/Cargo.toml`: `version = "1.0.0-beta.1"`
    2. Create a custom app icon (1024x1024 PNG) with a scissors/paper theme:
       - Design: Clean, modern icon featuring stylized scissors cutting paper/document
       - Color palette: Use the app's primary colors (blue/purple tones from the existing theme)
       - Style: Flat/material design with subtle shadows for depth
       - Create the source PNG programmatically using a Node.js script with the `canvas` package, or create an SVG and convert it
       - Alternative: Create a simple, bold SVG icon and use `sharp` (already in the project) to render to PNG
    3. Run `npx tauri icon src-tauri/icons/icon.png` to generate all required icon sizes from the source PNG.
    4. Verify all icon files are generated in `src-tauri/icons/`.

    Claude's discretion: Exact icon design details — create something clean and recognizable. Scissors cutting/snipping paper is the theme.
  </action>
  <verify>
    `grep '"version"' src-tauri/tauri.conf.json package.json` shows `1.0.0-beta.1` in both.
    `grep '^version' src-tauri/Cargo.toml` shows `1.0.0-beta.1`.
    `ls src-tauri/icons/icon.png` exists and is a valid PNG.
  </verify>
  <done>Version is 1.0.0-beta.1 everywhere. Custom icon generated in all required sizes.</done>
</task>

<task type="auto">
  <name>Task 2: Splash screen React overlay</name>
  <files>
    src/components/SplashScreen.tsx
    src/App.tsx
  </files>
  <action>
    1. Create `src/components/SplashScreen.tsx`:
       - Full-screen fixed overlay (z-50) with the app's background color
       - Center the app icon/logo and "Papercut" text
       - Add a subtle fade-in animation on mount
       - Accept an `onComplete` callback prop
       - After 1.5 seconds, trigger a fade-out animation (300ms), then call `onComplete`
       - Use Tailwind for all styling
       - Support dark mode (use existing CSS vars/theme classes)
    2. Integrate in `App.tsx`:
       - Add `showSplash` state (default: `true`)
       - Render `<SplashScreen onComplete={() => setShowSplash(false)} />` when `showSplash` is true
       - When splash completes, render the normal app content
       - Ensure splash doesn't interfere with existing app initialization

    Per research recommendation: Use React overlay approach — simpler than Tauri multi-window, works consistently across platforms.

    Claude's discretion: Exact splash screen visual design and timing.
  </action>
  <verify>
    File `src/components/SplashScreen.tsx` exists with component implementation.
    `grep "SplashScreen" src/App.tsx` shows integration.
    `npx tsc --noEmit` passes.
  </verify>
  <done>Splash screen shows app logo for ~1.5s on launch with fade-out transition before revealing main app.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify branding (icon, splash, version)</name>
  <files>src-tauri/tauri.conf.json</files>
  <action>
    Human verification of visual branding elements:
    1. Launch the app with `npm run tauri dev`
    2. Verify splash screen appears with logo and "Papercut" text, then fades out
    3. Check the app icon in the dock/taskbar — should show scissors/paper theme
    4. Verify version shows 1.0.0-beta.1 in tauri.conf.json
  </action>
  <verify>Visual inspection — splash screen, icon, and version all present and correct.</verify>
  <done>Splash screen displays on launch, custom icon visible in dock, version reads 1.0.0-beta.1.</done>
</task>

</tasks>

<verification>
- Version is `1.0.0-beta.1` in tauri.conf.json, package.json, and Cargo.toml
- Custom icon files exist in src-tauri/icons/
- Splash screen component renders on app launch
- TypeScript compilation passes
</verification>

<success_criteria>
App launches with a branded splash screen showing a custom scissors/paper icon. Version reads 1.0.0-beta.1.
</success_criteria>

<output>
After completion, create `.planning/phases/15-release-preparation/15-02-SUMMARY.md`
</output>
