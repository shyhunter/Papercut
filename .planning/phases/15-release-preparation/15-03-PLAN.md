---
phase: 15-release-preparation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - LICENSE
  - .github/ISSUE_TEMPLATE/bug_report.yml
  - .github/ISSUE_TEMPLATE/feature_request.yml
  - .github/pull_request_template.md
  - CHANGELOG.md
autonomous: true
requirements:
  - REPO-SETUP

must_haves:
  truths:
    - "README has hero description, features list, install instructions, and privacy section"
    - "MIT license file exists at project root"
    - "Bug report and feature request issue templates exist"
    - "PR template with checklist exists"
    - "CHANGELOG.md exists with initial release entry"
  artifacts:
    - path: "README.md"
      provides: "Full project README"
      min_lines: 80
    - path: "LICENSE"
      provides: "MIT license"
      contains: "MIT License"
    - path: ".github/ISSUE_TEMPLATE/bug_report.yml"
      provides: "Bug report template"
    - path: ".github/ISSUE_TEMPLATE/feature_request.yml"
      provides: "Feature request template"
    - path: ".github/pull_request_template.md"
      provides: "PR checklist template"
    - path: "CHANGELOG.md"
      provides: "Release changelog"
      contains: "1.0.0-beta.1"
  key_links: []
---

<objective>
Set up the GitHub repository for release: comprehensive README, MIT license, issue/PR templates, and CHANGELOG.

Purpose: Make the repository professional and contributor-ready for closed beta. Testers need install instructions, a way to report bugs, and clear project information.

Output: README, LICENSE, issue templates, PR template, CHANGELOG — all ready for beta.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-release-preparation/15-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README and LICENSE</name>
  <files>
    README.md
    LICENSE
  </files>
  <action>
    1. Create `LICENSE` file with MIT License text. Copyright holder: the project owner (use placeholder "Papercut Contributors" if name unknown).
    2. Rewrite `README.md` completely (replace the Tauri boilerplate):
       - **Hero section:** "Papercut" title, one-line tagline ("Your local document toolkit — private, fast, offline"), badges (version, license, platform)
       - **Screenshot placeholder:** `<!-- TODO: Add hero screenshot -->`
       - **Features section:** Organized by category:
         - PDF Tools: Compress, Resize, Merge, Split, Rotate, Page Numbers, Watermark, Crop, Organize, Sign, Redact, PDF/A, Repair, Protect, Unlock
         - Image Tools: Compress, Resize, Convert (JPG/PNG/WebP), Rotate
         - Document Tools: Convert (PDF/DOCX/EPUB/MOBI/etc.), Edit PDF
       - **Privacy section:** Bold statement — "Papercut processes everything on YOUR machine. No uploads, no cloud, no telemetry. Your files never leave your computer."
       - **Install section:** Download links (placeholder URLs to GitHub Releases), platform badges (macOS Apple Silicon, macOS Intel, Windows, Linux)
       - **Optional dependencies:** Table showing Ghostscript (for PDF compression/PDF-A/repair), LibreOffice (for DOC/DOCX conversion), Calibre (for ebook formats) with install commands per platform
       - **Development section:** `npm install`, `npm run tauri dev`, `npm run test`, `npx tsc --noEmit`
       - **Tech stack section:** Tauri v2, React, TypeScript, pdf-lib, Sharp, pdfjs-dist
       - **License section:** MIT — link to LICENSE file
       - **Contributing:** Brief note pointing to PR template
  </action>
  <verify>
    `wc -l README.md` shows 80+ lines.
    `head -1 LICENSE` shows "MIT License".
    `grep -c "Privacy" README.md` shows at least 1 match.
    `grep "1.0.0-beta" README.md` finds version mention.
  </verify>
  <done>README has full project description, features, install instructions, privacy statement, and dev setup. MIT LICENSE exists.</done>
</task>

<task type="auto">
  <name>Task 2: Issue templates, PR template, and CHANGELOG</name>
  <files>
    .github/ISSUE_TEMPLATE/bug_report.yml
    .github/ISSUE_TEMPLATE/feature_request.yml
    .github/pull_request_template.md
    CHANGELOG.md
  </files>
  <action>
    1. Create `.github/ISSUE_TEMPLATE/bug_report.yml` (YAML form format, not markdown):
       - Fields: description (textarea, required), steps to reproduce (textarea, required), expected behavior (textarea, required), actual behavior (textarea, required), system info section (OS, app version, Ghostscript version — all dropdowns or text inputs), screenshots (optional textarea)
       - Labels: `bug`
       - Title prefix: `[Bug] `

    2. Create `.github/ISSUE_TEMPLATE/feature_request.yml`:
       - Fields: description (textarea, required), use case (textarea, required), proposed solution (textarea, optional), alternatives considered (textarea, optional)
       - Labels: `enhancement`
       - Title prefix: `[Feature] `

    3. Create `.github/pull_request_template.md`:
       - Checklist: Description of changes, screenshots (if UI), tests added/updated, `npx tsc --noEmit` passes, `npm run test` passes, `npm run lint` passes
       - Related issue reference field

    4. Create `CHANGELOG.md`:
       - Follow Keep a Changelog format (https://keepachangelog.com)
       - Add `[1.0.0-beta.1]` section with "Initial beta release" and key feature highlights organized by category (Added section)
       - List the major feature areas: 21 tools, PDF/image/document processing, privacy-first architecture, dark/light theme, E2E tests
  </action>
  <verify>
    `ls .github/ISSUE_TEMPLATE/bug_report.yml .github/ISSUE_TEMPLATE/feature_request.yml .github/pull_request_template.md CHANGELOG.md` — all exist.
    `grep "1.0.0-beta.1" CHANGELOG.md` finds the release entry.
  </verify>
  <done>Issue templates (bug + feature), PR template, and CHANGELOG with beta.1 entry all exist.</done>
</task>

</tasks>

<verification>
- README.md is comprehensive (80+ lines, features, privacy, install)
- LICENSE contains MIT text
- Bug report and feature request YAML templates exist in .github/ISSUE_TEMPLATE/
- PR template exists with checklist
- CHANGELOG.md has 1.0.0-beta.1 entry
</verification>

<success_criteria>
Repository is professional and beta-ready with documentation, license, templates, and changelog.
</success_criteria>

<output>
After completion, create `.planning/phases/15-release-preparation/15-03-SUMMARY.md`
</output>
