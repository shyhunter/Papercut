---
phase: 15-release-preparation
plan: 06
type: execute
wave: 2
depends_on: ["15-02"]
files_modified:
  - src/styles/globals.css
  - src/components/Dashboard.tsx
  - src/App.tsx
  - src/components/ConfigureStep.tsx
  - src/components/ImageConfigureStep.tsx
  - src/components/CompareStep.tsx
  - src/components/ImageCompareStep.tsx
  - src/components/SaveStep.tsx
  - src/App.css
autonomous: false
requirements:
  - UI-POLISH

must_haves:
  truths:
    - "Dark mode looks polished across all tools with no visual glitches"
    - "Dashboard has refined spacing, hover effects, and category headers"
    - "Processing operations show clear loading states with progress indicators"
    - "Buttons have consistent styling across all views"
    - "UI scales reasonably across different window sizes"
    - "Success states have subtle animations for delight"
  artifacts:
    - path: "src/styles/globals.css"
      provides: "Refined dark mode CSS variables and transitions"
    - path: "src/components/Dashboard.tsx"
      provides: "Polished dashboard layout"
  key_links: []
---

<objective>
Polish the UI for release: dark mode refinement, dashboard layout improvements, loading states, button consistency, responsive design, and micro-interactions.

Purpose: The app needs to feel professional and polished for beta testers. UI rough edges should be smoothed out across all tools and views.

Output: Refined, consistent UI across all components with dark mode, animations, and responsive behavior.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/styles/globals.css
@src/components/Dashboard.tsx
@src/App.css
@.planning/phases/15-release-preparation/15-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dark mode refinement and button/component consistency</name>
  <files>
    src/styles/globals.css
    src/App.css
    src/components/Dashboard.tsx
    src/components/ConfigureStep.tsx
    src/components/ImageConfigureStep.tsx
    src/components/SaveStep.tsx
  </files>
  <action>
    1. **Dark mode refinement in globals.css:**
       - Audit all `.dark` CSS variable overrides for consistency
       - Ensure borders, shadows, and hover states have appropriate dark variants
       - Add subtle transition on theme change: `transition: background-color 0.2s, color 0.2s` on root elements
       - Check that card backgrounds, input fields, and dropdowns have good dark mode contrast

    2. **Dashboard layout polish:**
       - Refine tool card spacing (consistent gap between cards)
       - Add hover scale transform on tool cards: `hover:scale-[1.02]` with `transition-transform`
       - Ensure category headers have proper typography hierarchy
       - Add subtle card shadows that differ between light and dark mode
       - Quick action pills should have consistent sizing and hover effects

    3. **Button consistency audit:**
       - Review all primary action buttons across Configure, Compare, and Save steps
       - Ensure consistent border radius, padding, font size, and color scheme
       - Primary buttons: filled style, consistent hover/active states
       - Secondary buttons: outlined style, consistent hover
       - Disabled state should be visually distinct (opacity + cursor)
       - Add focus-visible outlines for keyboard accessibility

    4. **Responsive improvements:**
       - Verify the existing `clamp()` root font-size works well (from Phase 8)
       - Ensure Dashboard grid collapses gracefully on smaller windows
       - Configure/Compare steps should stack panels vertically on narrow windows
       - Test that minimum window size (600x440) doesn't cause overflow/clipping
  </action>
  <verify>
    `npx tsc --noEmit` passes.
    `grep "transition" src/styles/globals.css` shows theme transition.
    `grep "hover:scale" src/components/Dashboard.tsx` shows hover effect.
  </verify>
  <done>Dark mode is polished. Buttons are consistent. Dashboard has hover effects and refined spacing. Responsive behavior is solid.</done>
</task>

<task type="auto">
  <name>Task 2: Loading states and success animations</name>
  <files>
    src/components/CompareStep.tsx
    src/components/ImageCompareStep.tsx
    src/components/SaveStep.tsx
    src/styles/globals.css
    src/App.tsx
  </files>
  <action>
    1. **Loading/progress state improvements:**
       - Review existing processing spinners in CompareStep and ImageCompareStep
       - Add a progress bar component or improve existing ones (show percentage when available)
       - Skeleton loading placeholders for preview panels while PDF/image is rendering
       - Ensure the "Processing..." state is visually clear and communicates activity

    2. **Success animations (delight features):**
       - Save completion: Add a checkmark animation (CSS keyframe, scale-in + check draw) in SaveStep
       - File drop: Subtle bounce animation when file is accepted in LandingCard
       - Tool selection: Smooth transition when entering a tool from Dashboard
       - Processing complete: Gentle fade-in of the Compare view results
       - Use CSS `@keyframes` — no animation library needed
       - All animations should be subtle (200-400ms) and not block interaction

    3. **Add animation keyframes to globals.css:**
       - `@keyframes checkmark-draw` — for save success
       - `@keyframes bounce-in` — for file drop acceptance
       - `@keyframes fade-slide-in` — for content appearance
       - Respect `prefers-reduced-motion` media query — disable animations for accessibility

    4. **Info sharing capability (from context decisions):**
       - Add a "Copy result info" or "Share stats" button in CompareStep/SaveStep
       - Copies processing stats (original size → processed size, reduction %) to clipboard
       - Use Tauri clipboard API or `navigator.clipboard.writeText`
  </action>
  <verify>
    `npx tsc --noEmit` passes.
    `grep "@keyframes" src/styles/globals.css` shows animation definitions.
    `grep "prefers-reduced-motion" src/styles/globals.css` shows a11y support.
  </verify>
  <done>Processing shows clear progress indicators. Success states have subtle animations. Animations respect prefers-reduced-motion.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify UI polish across all views</name>
  <files>src/components/Dashboard.tsx</files>
  <action>
    Human verification of UI polish:
    1. Launch app with `npm run tauri dev`
    2. Toggle dark mode — verify smooth transition, no visual glitches
    3. Check Dashboard: tool cards should have hover scale effect, consistent spacing
    4. Open a PDF tool, process a file — verify loading states and progress display
    5. Save a file — verify success animation (checkmark)
    6. Resize the window to minimum size — verify no overflow/clipping
    7. Try file drop — verify subtle bounce on acceptance
    8. Check button styles across different steps — should be consistent
  </action>
  <verify>Visual inspection across dark/light mode, all steps, and window sizes.</verify>
  <done>Dark mode polished, hover effects working, buttons consistent, animations present, responsive behavior solid.</done>
</task>

</tasks>

<verification>
- Dark mode renders well across all views
- Dashboard has hover effects and refined layout
- Buttons are consistent across all steps
- Processing shows progress indicators
- Success states have subtle animations
- Animations respect prefers-reduced-motion
- UI scales with window size
- TypeScript compilation passes
</verification>

<success_criteria>
The app looks and feels polished. Dark mode is refined. Interactions have subtle delight. Layout is responsive and consistent.
</success_criteria>

<output>
After completion, create `.planning/phases/15-release-preparation/15-06-SUMMARY.md`
</output>
