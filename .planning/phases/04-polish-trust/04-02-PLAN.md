---
phase: 04-polish-trust
plan: 02
type: tdd
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/lib/__tests__/privacy.test.ts
autonomous: true
requirements:
  - UX-03

must_haves:
  truths:
    - "A test verifies that capabilities/default.json contains zero http:* permissions"
    - "A test verifies that running a processing function never calls window.fetch"
    - "Both tests pass in CI (npm test) without a Tauri runtime"
  artifacts:
    - path: "src/lib/__tests__/privacy.test.ts"
      provides: "Two privacy tests: static config assertion + runtime fetch spy"
      exports: ["capabilities grant no HTTP access", "processing never calls window.fetch"]
  key_links:
    - from: "src/lib/__tests__/privacy.test.ts"
      to: "src-tauri/capabilities/default.json"
      via: "readFileSync — static config test reads the JSON file directly"
      pattern: "readFileSync.*capabilities/default\\.json"
    - from: "src/lib/__tests__/privacy.test.ts"
      to: "src/lib/imageProcessor.ts"
      via: "vi.mock('@tauri-apps/api/core') + invoke stub — runtime fetch spy test"
      pattern: "vi\\.stubGlobal.*fetch"
---

<objective>
Write the two automated privacy tests that prove no outbound network calls occur — one static (config assertion) and one runtime (fetch intercept). These are the developer-facing half of the privacy verification; the user-facing half (PrivacyFooter) was delivered in Plan 04-01.

Purpose: Provides a reproducible, CI-runnable proof that the app's privacy guarantees hold. Closes UX-03.

Output: `src/lib/__tests__/privacy.test.ts` — two passing Vitest tests, no Tauri runtime required.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@src-tauri/capabilities/default.json
@src/lib/imageProcessor.ts
@src/lib/pdfProcessor.ts
@src/setupTests.ts
</context>

<feature>
  <name>Privacy verification — no outbound network calls</name>
  <files>src/lib/__tests__/privacy.test.ts</files>
  <behavior>
Test 1 — Static config assertion:
  - Read `src-tauri/capabilities/default.json` via `fs.readFileSync`
  - Extract all permission identifiers (handle both string entries and `{ identifier }` object entries)
  - Assert that zero identifiers start with `"http:"`
  - This proves the capabilities config structurally cannot grant outbound HTTP

Test 2 — Runtime fetch spy:
  - Stub `window.fetch` via `vi.stubGlobal('fetch', vi.fn())`
  - Mock `@tauri-apps/api/core` invoke to return a minimal valid image result (use existing mock pattern from setupTests.ts)
  - Import and call the `processImage` function from `src/lib/imageProcessor.ts`
  - Assert `vi.mocked(fetch)` was never called
  - This proves the image processing code path never touches `window.fetch`

Edge cases to cover:
  - The `permissions` array may contain strings OR `{ identifier, allow }` objects — both must be normalized
  - The fetch mock must be restored after the test (vi.unstubAllGlobals in afterEach or afterAll)
  </behavior>
  <implementation>
Follow the RED → GREEN → REFACTOR cycle:

RED: Create the test file with both tests failing (since the test file doesn't exist yet). The static test will fail until capabilities/default.json path is correctly resolved. The runtime spy test will fail if the imageProcessor actually called fetch (it does not — but the test structure must be in place first).

GREEN: Fix path resolution (use `path.join(__dirname, '../../../src-tauri/capabilities/default.json')` — this resolves correctly relative to `src/lib/__tests__/`). Ensure the invoke mock matches the existing `setup.ts` pattern so the processImage call completes without error.

REFACTOR: Clean up any repeated path setup into a `beforeAll`, confirm both test descriptions are clear about what they assert.

Path to capabilities.json from `src/lib/__tests__/privacy.test.ts`:
```typescript
import path from 'path';
const capPath = path.join(__dirname, '../../../src-tauri/capabilities/default.json');
```
That resolves to: `src/lib/__tests__/ → ../../../ → project root → src-tauri/capabilities/default.json` ✓

The invoke mock must return a `Uint8Array` (since `processImage` uses `invoke` with `TAURI_INVOKE_KEY` raw response). Check what `src/setupTests.ts` already does to mock `@tauri-apps/api/core` — replicate or extend that pattern. Do NOT create a second conflicting mock.

Commit after each phase:
- RED: `test(04-02): add failing privacy tests`
- GREEN: `feat(04-02): privacy tests pass`
- REFACTOR (if needed): `refactor(04-02): clean up privacy test setup`
  </implementation>
</feature>

<verification>
`npm test` (from project root) passes — both privacy tests show as passing:
- "capabilities grant no HTTP access"
- "processing never calls window.fetch"

No other tests regressed.
</verification>

<success_criteria>
- `npm test` exits 0
- Two new passing tests in `privacy.test.ts`
- Static test reads real `capabilities/default.json` — not a mock of the file
- Runtime test uses `vi.stubGlobal` + `vi.mocked(fetch).not.toHaveBeenCalled()` pattern
- Tests run without Tauri window/runtime (pure Node.js Vitest execution)
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-trust/04-02-SUMMARY.md`
</output>
