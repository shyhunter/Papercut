---
phase: 12-advanced-pdf-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/tools.ts
  - src/App.tsx
  - src/assets/fonts/dancing-script.woff2
  - src/assets/fonts/caveat.woff2
  - src/assets/fonts/great-vibes.woff2
  - src/assets/fonts.css
  - src/components/shared/PagePreview.tsx
autonomous: true
requirements: [SC-05]

must_haves:
  truths:
    - "Dashboard shows Sign PDF, Redact PDF, PDF/A, and Repair PDF tool cards"
    - "Clicking any of the 4 new tools enters a placeholder flow"
    - "PagePreview renders a PDF page to canvas with an overlay slot"
  artifacts:
    - path: "src/types/tools.ts"
      provides: "4 new ToolId entries with ToolDefinition"
      contains: "sign-pdf"
    - path: "src/App.tsx"
      provides: "Routing for 4 new tools"
      contains: "sign-pdf"
    - path: "src/components/shared/PagePreview.tsx"
      provides: "Reusable page preview with overlay"
      exports: ["PagePreview"]
    - path: "src/assets/fonts.css"
      provides: "3 signature font-face declarations"
      contains: "@font-face"
  key_links:
    - from: "src/App.tsx"
      to: "src/types/tools.ts"
      via: "activeTool routing"
      pattern: "activeTool === 'sign-pdf'"
---

<objective>
Register 4 new advanced PDF tools in the tool registry, add App.tsx routing stubs, bundle signature fonts, and create a shared PagePreview component.

Purpose: Establish the foundation (registry, routing, shared components) that all 4 advanced tools depend on.
Output: Dashboard shows 4 new tool cards; PagePreview component ready for Sign and Redact tools; signature fonts available for typed signature mode.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-advanced-pdf-tools/12-CONTEXT.md
@.planning/phases/12-advanced-pdf-tools/12-RESEARCH.md
@src/types/tools.ts
@src/App.tsx
@src/lib/pdfThumbnail.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register 4 new tools and add routing stubs</name>
  <files>src/types/tools.ts, src/App.tsx</files>
  <action>
    Add 4 new ToolId values to the union type: 'sign-pdf', 'redact-pdf', 'pdfa-convert', 'repair-pdf'.

    Add 4 new entries to TOOL_REGISTRY:
    - sign-pdf: name "Sign PDF", description "Add a visual signature stamp to PDF pages", category 'pdf', icon 'PenTool', acceptsFormats ['pdf'], steps: [Select PDF, Signature, Place, Save] (4 steps per user decision)
    - redact-pdf: name "Redact PDF", description "Permanently remove text or areas from a PDF", category 'pdf', icon 'EyeOff', acceptsFormats ['pdf'], steps: [Select PDF, Redact, Save] (3 steps per user decision)
    - pdfa-convert: name "PDF/A Convert", description "Convert PDF to archival PDF/A format", category 'pdf', icon 'Archive', acceptsFormats ['pdf'], steps: [Select PDF, Configure, Save] (3 steps)
    - repair-pdf: name "Repair PDF", description "Fix corrupted or malformed PDFs", category 'pdf', icon 'Wrench', acceptsFormats ['pdf'], steps: [Select PDF, Repair, Save] (3 steps)

    In App.tsx, add routing stubs for each new tool in the ToolFlow component, following the existing pattern (e.g., the protect-pdf block). For now, render a simple placeholder div with the tool name and "Coming soon" text. Import placeholder components or use inline JSX -- these will be replaced by actual flow components in later plans.
  </action>
  <verify>Run `npx tsc --noEmit` -- no type errors. Run `npm run dev` and confirm all 4 tools appear on dashboard.</verify>
  <done>Dashboard shows Sign PDF, Redact PDF, PDF/A Convert, and Repair PDF cards. Clicking each enters a placeholder view.</done>
</task>

<task type="auto">
  <name>Task 2: Bundle signature fonts and create shared PagePreview component</name>
  <files>src/assets/fonts/dancing-script.woff2, src/assets/fonts/caveat.woff2, src/assets/fonts/great-vibes.woff2, src/assets/fonts.css, src/components/shared/PagePreview.tsx</files>
  <action>
    **Signature fonts:**
    Download 3 OFL-licensed signature fonts in WOFF2 format and place in src/assets/fonts/:
    1. Dancing Script (flowing script) -- from Google Fonts or fontsource
    2. Caveat (casual handwriting)
    3. Great Vibes (formal script)

    Use `curl` to download from fontsource CDN or Google Fonts API. If direct download fails, use npm packages (@fontsource/dancing-script, @fontsource/caveat, @fontsource/great-vibes) and copy the woff2 files, or generate fallback by creating a simple CSS-only approach using system cursive fonts as backup.

    Create src/assets/fonts.css with @font-face declarations for all 3 fonts, referencing the woff2 files. Import this CSS file in src/main.tsx or src/App.css.

    **PagePreview component:**
    Create src/components/shared/PagePreview.tsx -- a reusable component that:
    - Props: pdfBytes (Uint8Array), pageIndex (number), scale (number, default 1.5), children (ReactNode for overlay), className (optional)
    - Renders the specified PDF page to an HTML canvas using pdfjs-dist (follow pdfThumbnail.ts patterns, use pdfBytes.slice() for StrictMode safety)
    - Wraps canvas in a relative-positioned container with dimensions matching the rendered page
    - Renders children as an absolute-positioned overlay on top of the canvas (this is where Sign and Redact tools will place interactive elements)
    - Exposes page dimensions (width, height at current scale) via a callback prop: onDimensionsReady?: (dims: { width: number; height: number; pdfWidth: number; pdfHeight: number }) => void
    - Uses cancelled flag pattern for async effect cleanup
    - Handles loading state (show a spinner or skeleton)
  </action>
  <verify>Run `npx tsc --noEmit`. Verify fonts.css is imported and font-face declarations are valid. Verify PagePreview component renders a PDF page (can test by temporarily using it in a placeholder stub).</verify>
  <done>3 signature fonts available via CSS font-family. PagePreview component renders PDF pages with overlay slot and reports dimensions.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Dashboard shows 16 tool cards (12 existing + 4 new)
- Clicking each new tool shows placeholder content
- PagePreview renders a PDF page correctly
- Signature fonts load in CSS
</verification>

<success_criteria>
All 4 new tools appear on dashboard. PagePreview component and signature fonts are ready for use by subsequent plans.
</success_criteria>

<output>
After completion, create `.planning/phases/12-advanced-pdf-tools/12-01-SUMMARY.md`
</output>
