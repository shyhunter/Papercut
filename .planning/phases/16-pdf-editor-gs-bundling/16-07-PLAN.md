---
phase: 16-pdf-editor-gs-bundling
plan: 07
type: execute
wave: 4
depends_on: ["16-01", "16-06"]
files_modified:
  - src/App.tsx
  - src/components/pdf-editor/EditorView.tsx
  - src/components/Dashboard.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "All existing step-by-step tool flows remain functional alongside the new editor"
    - "Editor view is accessible from dashboard and via file association"
    - "GS-dependent tools work with bundled sidecar binary"
    - "Navigating between editor and dashboard preserves state correctly"
  artifacts:
    - path: "src/App.tsx"
      provides: "Correct routing: dashboard, tool flows, and editor coexist"
  key_links:
    - from: "src/components/Dashboard.tsx"
      to: "src/components/pdf-editor/EditorView.tsx"
      via: "Edit PDF card or Open in Editor action"
      pattern: "openEditor|EditorView"
---

<objective>
Integration verification — ensure all existing tool flows work alongside the new editor, GS sidecar is functional, and the full editor experience is polished.

Purpose: Final integration pass to catch regressions, verify the GS bundling works end-to-end, and ensure the editor is accessible from both dashboard and file association.
Output: Verified integration, any fixups needed, checkpoint for visual/functional verification.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/16-pdf-editor-gs-bundling/16-01-SUMMARY.md
@.planning/phases/16-pdf-editor-gs-bundling/16-02-SUMMARY.md
@.planning/phases/16-pdf-editor-gs-bundling/16-03-SUMMARY.md
@.planning/phases/16-pdf-editor-gs-bundling/16-04-SUMMARY.md
@.planning/phases/16-pdf-editor-gs-bundling/16-05-SUMMARY.md
@.planning/phases/16-pdf-editor-gs-bundling/16-06-SUMMARY.md

@src/App.tsx
@src/components/Dashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration fixups and dashboard wiring</name>
  <files>src/App.tsx, src/components/Dashboard.tsx, src/components/pdf-editor/EditorView.tsx</files>
  <action>
1. Verify and fix App.tsx routing:
   - Ensure three-way routing works: dashboard (default) | tool flow (selectTool) | editor (openEditor)
   - Verify that selecting "Edit PDF" tool from dashboard opens the NEW editor view (not the old Phase 13 EditPdfFlow)
   - Update the "Edit PDF" tool card in Dashboard to route to the new editor
   - Consider: the old EditPdfFlow from Phase 13 can remain as a simpler editing mode OR be replaced entirely. Decision: replace it — the new editor subsumes all Phase 13 edit-pdf functionality. Update TOOL_REGISTRY entry for 'edit-pdf' to point to new editor.

2. Verify GS-dependent tools work:
   - Test compress-pdf with bundled GS sidecar
   - Test protect-pdf and unlock-pdf
   - Test pdfa-convert and repair-pdf
   - If any fail, fix the sidecar resolution path

3. Verify existing tool flows are not broken:
   - Quick smoke test: open compress-pdf tool from dashboard → pick file → configure → compare → save
   - Quick smoke test: open compress-image tool → same flow
   - Quick smoke test: merge-pdf, split-pdf still work

4. Polish items:
   - Ensure editor properly unmounts and cleans up when navigating to dashboard
   - Ensure dashboard properly remounts when returning from editor
   - Check that pendingFiles flow still works for drag-drop on dashboard
  </action>
  <verify>
`npx tsc --noEmit` passes.
Manual test: dashboard shows all tools, compress-pdf flow works end-to-end, editor opens from both dashboard and file association.
  </verify>
  <done>All routing works. Existing tools are not regressed. GS sidecar is functional. Editor is accessible from dashboard.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of complete editor</name>
  <files>N/A</files>
  <action>Human verification of all Phase 16 features. Follow the steps in how-to-verify.</action>
  <verify>All 21 verification steps pass without issues.</verify>
  <done>Phase 16 is complete — all 8 success criteria verified by human.</done>
  <what-built>
Full PDF editor with:
- Continuous scroll canvas with zoom controls
- Text editing (click-to-select, double-click-to-edit, formatting toolbar)
- Page panel (thumbnails, drag-reorder, add/delete/duplicate, multi-select)
- Tool sidebar (icon strip with inline tool execution)
- File association (Open with Papercut → editor)
- Save model (Cmd+S, Cmd+Shift+S, unsaved changes guard)
- GS bundled as sidecar (no external install)
  </what-built>
  <how-to-verify>
1. Launch app (`npm run dev` from project root)
2. Verify dashboard loads and all tool cards are visible
3. Click "Edit PDF" → verify editor view opens
4. Open a multi-page PDF → verify all pages render in continuous scroll
5. Test zoom: Cmd++, Cmd+-, zoom toolbar presets (50%, 75%, 100%, 150%, Fit Width)
6. Click on text → verify selection box appears
7. Double-click text → verify inline editing with cursor
8. Change font/size/color/bold via top toolbar → verify text updates
9. Page panel (left): click thumbnail → canvas scrolls to page
10. Page panel: drag a thumbnail to reorder → verify pages reorder
11. Page panel: "Insert blank page" → verify new page appears
12. Page panel: select pages, click "Duplicate" → verify duplicates
13. Page panel: select pages, click "Delete" → verify removal
14. Tool sidebar (right): click compress icon → verify panel expands
15. Apply compression → verify canvas refreshes with compressed PDF
16. Cmd+S → verify save dialog (first time) → file saves
17. Make edits → Cmd+S → verify overwrites without dialog
18. Cmd+Shift+S → verify Save As dialog always opens
19. Make edits → click "Dashboard" breadcrumb → verify unsaved changes warning
20. From dashboard: run compress-pdf tool flow end-to-end → verify no regression
21. From dashboard: run compress-image tool flow → verify no regression
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- All 8 success criteria from the phase are met
- No regressions in existing tool flows
- GS sidecar works for compression and other GS tools
- Editor is functional and feels like a real document editor
</verification>

<success_criteria>
The complete Phase 16 is verified: editor view works with all features, tools are accessible inline, file association works, GS is bundled, and existing flows are preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/16-pdf-editor-gs-bundling/16-07-SUMMARY.md`
</output>
