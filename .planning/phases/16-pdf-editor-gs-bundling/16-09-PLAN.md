---
phase: 16-pdf-editor-gs-bundling
plan: 09
type: execute
wave: 6
depends_on: ["16-08"]
files_modified:
  - src/components/pdf-editor/FormattingToolbar.tsx
  - src/components/pdf-editor/TextEditingLayer.tsx
  - src/components/pdf-editor/ToolSidebarPanel.tsx
  - src/components/pdf-editor/ToolSidebarPreview.tsx
  - src/components/pdf-editor/EditorTopToolbar.tsx
  - src/components/pdf-editor/EditorView.tsx
  - src/components/pdf-editor/EditorCanvas.tsx
  - src/context/EditorContext.tsx
  - src/types/editor.ts
autonomous: false
requirements: []
must_haves:
  truths:
    - "Line spacing control is available in formatting toolbar"
    - "Rotate tool uses directional UI instead of degree numbers"
    - "Save confirmation shows clickable link to saved file"
    - "Pinch-to-zoom works in page view (EditorCanvas)"
    - "Live comparison editing mode available"
    - "File association works on macOS (Open With + re-open when already running)"
    - "Sign and redact tools verified working in editor sidebar context"
---

<objective>
Implement 5 new feature requests from user testing feedback. These are UX improvements and new capabilities not in the original Phase 16 plan.

Features:
1. **Line spacing controls** — add 0.5, 1.0, 1.5, 2.0 line-height options to FormattingToolbar
2. **Live comparison editing** — ability to edit while seeing before/after (dual-pane or overlay slider)
3. **Remove Organize from sidebar** — already done in 16-08 if needed
4. **Save confirmation with file link + recent files in editor** — link to saved file, recent file access
5. **Rotate UI redesign** — compass-style directional buttons (N/E/S/W = 0°/90°/180°/270°) instead of degree input
</objective>

<context>
@.planning/phases/16-pdf-editor-gs-bundling/16-08-PLAN.md
@src/components/pdf-editor/FormattingToolbar.tsx
@src/components/pdf-editor/ToolSidebarPanel.tsx
@src/components/pdf-editor/EditorTopToolbar.tsx
@src/components/pdf-editor/EditorView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add line spacing to FormattingToolbar</name>
  <files>src/components/pdf-editor/FormattingToolbar.tsx, src/types/editor.ts, src/components/pdf-editor/TextEditingLayer.tsx</files>
  <action>
Add a line spacing dropdown to FormattingToolbar alongside existing font/size/color controls.

1. Add `lineHeight` property to TextBlock type in editor.ts (default: 1.2)
2. Add a dropdown in FormattingToolbar with options: 0.5, 1.0, 1.2 (default), 1.5, 2.0
3. Display as a small dropdown with a line-spacing icon (parallel horizontal lines with arrows)
4. Apply via `updateTextBlock()` with `lineHeight` property
5. In TextEditingLayer, apply `lineHeight` CSS to text block container divs
6. Edited lines should automatically respect the line height and not break margins

Keep the control compact — it should fit in the existing toolbar row without overflow.
  </action>
  <verify>
- Select a text block → change line spacing → text visually adjusts spacing
- New text blocks default to 1.2 line height
- `npx tsc --noEmit` passes
  </verify>
  <done>Line spacing control works in toolbar and visually affects text blocks.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign rotate tool with directional compass UI</name>
  <files>src/components/pdf-editor/ToolSidebarPanel.tsx</files>
  <action>
Replace the numeric degree input in the Rotate panel with a visual compass-style UI.

1. Show 4 directional buttons arranged in a cross/compass layout:
   - Top: ↑ "Original" (0°)
   - Right: → "Turn Right" (90°)
   - Bottom: ↓ "Upside Down" (180°)
   - Left: ← "Turn Left" (270°)
2. Highlight the currently selected rotation direction
3. Clicking a direction immediately updates the preview
4. Keep the "Apply to: Current page / Selected pages / All pages" scope selector
5. The visual should make it obvious which direction the page will face

The current numeric approach confuses users who rotated a page and couldn't figure out how to rotate it back. The compass makes all 4 states visible and one-click accessible.
  </action>
  <verify>
- Rotate panel shows 4 directional buttons instead of degree input
- Clicking each direction shows correct preview rotation
- Clicking "Original" returns page to normal orientation
- Apply works correctly for all 4 directions
  </verify>
  <done>Rotate tool uses intuitive compass-style directional buttons.</done>
</task>

<task type="auto">
  <name>Task 3: Live comparison editing mode</name>
  <files>src/components/pdf-editor/EditorView.tsx, src/components/pdf-editor/EditorCanvas.tsx, src/context/EditorContext.tsx, src/types/editor.ts</files>
  <action>
Add a comparison mode that shows the original PDF alongside the edited version while the user continues editing.

Recommended UX approach: **Side-by-side split view with overlay slider option**

1. Add a "Compare" toggle button in EditorTopToolbar (eye icon or split-screen icon)
2. When activated, the canvas area splits into two panes:
   - Left: Original PDF (read-only, synced scroll position)
   - Right: Edited PDF (full editing capabilities maintained)
3. Both panes scroll in sync (scroll one, both scroll)
4. A vertical divider between panes can be dragged to adjust split ratio
5. Alternative mode: overlay slider (single canvas, drag a vertical line left/right to reveal original vs edited) — add as a toggle between "Split" and "Slider" modes
6. Pressing "Compare" again exits comparison mode

State additions to EditorContext:
- `compareMode: 'off' | 'split' | 'slider'`
- `originalPdfBytes: Uint8Array | null` — snapshot of PDF at editor open time (never modified)

The originalPdfBytes should be captured once when the PDF is first loaded and never updated, even as edits are made. This provides a stable "before" reference.
  </action>
  <verify>
- Toggle compare mode → canvas splits into original (left) and edited (right)
- Scrolling one pane scrolls the other in sync
- Editing text in right pane works normally while compare mode is active
- Toggle slider mode → single canvas with draggable divider line
- Exit compare mode → back to single canvas editing
  </verify>
  <done>Live comparison editing works in both split and slider modes.</done>
</task>

<task type="auto">
  <name>Task 4: Save confirmation with file link and recent files access</name>
  <files>src/components/pdf-editor/SaveController.tsx, src/components/pdf-editor/EditorTopToolbar.tsx</files>
  <action>
After saving, show a confirmation toast/banner with a clickable link to open the saved file location.

1. After successful save (Cmd+S or Save As), show a non-blocking toast at the bottom of the editor:
   - "Saved to filename.pdf" with a clickable "Show in Finder" link
   - Auto-dismiss after 5 seconds, or manual dismiss with X
2. "Show in Finder" calls `invoke('reveal_in_finder', { path })` to open the file's directory in Finder
3. Add a "Recent files" dropdown in EditorTopToolbar breadcrumb area:
   - Shows last 5 files opened in the editor
   - Clicking one opens it in the editor (with unsaved-changes guard if dirty)
   - Uses the existing `useRecentDirs` store or a new `useRecentEditorFiles` store
4. The saved file path should be tracked so subsequent Cmd+S saves to the same location without dialog

Note: `reveal_in_finder` Tauri command already exists in the backend.
  </action>
  <verify>
- Save a file → toast shows "Saved to X" with "Show in Finder" link
- Click "Show in Finder" → Finder opens to the file's directory
- Toast auto-dismisses after 5 seconds
- Recent files dropdown shows previously opened files
  </verify>
  <done>Save shows file link. Recent files accessible from editor toolbar.</done>
</task>

<task type="auto">
  <name>Task 5: Verify pinch-to-zoom works in editor canvas</name>
  <files>src/components/pdf-editor/EditorCanvas.tsx</files>
  <action>
Pinch-to-zoom IS implemented in EditorCanvas (wheel+ctrlKey + gesturechange events) but user reports it doesn't work in page view.

Investigate:
1. Check if the wheel event listener is registered with `{ passive: false }` — required for `preventDefault()` to work
2. Check if the gesture events are only registered in the compare view (they shouldn't be)
3. Verify the zoom update from pinch feeds back into the ZoomToolbar display
4. Test: does the zoom toolbar +/- indicator update when pinching?

The user says "It only working by comparison view" — this suggests the event listeners might be conditionally registered or the canvas container ref changes between views.

Fix any issues found. Ensure pinch-to-zoom and toolbar zoom are always in sync.
  </action>
  <verify>
- Pinch in/out on trackpad → canvas zoom changes smoothly
- Zoom toolbar percentage updates in sync with pinch zoom
- Toolbar +/- buttons and pinch zoom are interchangeable
  </verify>
  <done>Pinch-to-zoom works consistently in page view and syncs with toolbar.</done>
</task>

<task type="auto">
  <name>Task 6: Fix macOS file association for "Open With" Papercut</name>
  <files>src-tauri/src/lib.rs, src/App.tsx</files>
  <action>
File association config in tauri.conf.json is correct, but has gaps:

1. **In dev mode:** File associations don't work — macOS only registers them for installed apps. This is expected. Document this.
2. **When app is already running:** macOS sends an Apple Event (`open-file`) that Tauri v2 surfaces as `RunEvent::Opened`. The current Rust code only handles CLI args at launch. Add a `RunEvent::Opened` handler in the `.run()` closure to emit `file-opened` to the frontend.
3. **Verify in production build:** Build with `npm run tauri build`, install the .dmg, then test:
   - Right-click PDF → "Open With" → Papercut
   - Set Papercut as default PDF app
   - Double-click a PDF → Papercut opens and loads it

Rust change in lib.rs:
```rust
.run(tauri::generate_context!())
```
→ Change to use a run callback that handles `RunEvent::Opened { urls }` and emits "file-opened" for PDF paths.
  </action>
  <verify>
- `cargo build` compiles with RunEvent::Opened handler
- Production build: right-click PDF → Open With Papercut → file opens in editor
- Production build: double-click PDF when Papercut already running → file opens in editor
  </verify>
  <done>macOS file association works for initial launch and when app is already running.</done>
</task>

<task type="auto">
  <name>Task 7: Verify sign and redact tools in editor sidebar</name>
  <files>src/components/pdf-editor/ToolSidebarPanel.tsx</files>
  <action>
Sign and Redact panels currently show placeholder messages ("Use the full tool for now"). Verify:

1. **Sign PDF:** Does the full sign-pdf tool flow still work from the dashboard? If yes, either:
   - Route the sidebar icon to open the full tool flow in a modal/overlay, OR
   - Show a clear "Open Sign Tool" button that navigates to the dashboard tool
2. **Redact PDF:** Same check — does the full redact-pdf tool work? Add navigation button.
3. For both: update the placeholder text to be more actionable (button instead of just text)

Long-term goal (future phase): implement inline sign placement and inline redact-by-rectangle directly on the editor canvas. For now, ensure users can still access the full tools.
  </action>
  <verify>
- Click sign icon in sidebar → clear way to access sign functionality
- Click redact icon in sidebar → clear way to access redact functionality
- Full sign-pdf and redact-pdf tool flows work correctly from dashboard
  </verify>
  <done>Sign and redact are accessible from editor sidebar with clear navigation to full tools.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: User verification of new features</name>
  <files>N/A</files>
  <action>Human verification of all 5 new features.</action>
  <verify>All 5 features work as expected.</verify>
  <done>All new features verified by user.</done>
  <what-built>
7 features/fixes from user testing feedback:
1. Line spacing controls in formatting toolbar (0.5, 1.0, 1.2, 1.5, 2.0)
2. Compass-style rotate tool (↑↓←→ instead of degrees)
3. Live comparison editing mode (split view + overlay slider)
4. Save confirmation with "Show in Finder" link + recent files dropdown
5. Pinch-to-zoom verified/fixed in page view
6. macOS file association: "Open With" and "Set as Default" work in production build
7. Sign and redact tools accessible from editor sidebar with clear navigation
  </what-built>
  <how-to-verify>
1. Select text → change line spacing in toolbar → verify spacing changes visually
2. Open rotate tool → verify 4 directional buttons → rotate and return to original
3. Click Compare toggle → verify split view with synced scroll → edit text in right pane
4. Toggle to slider mode → verify overlay slider works
5. Save file → verify toast with "Show in Finder" link → click it → Finder opens
6. Check recent files dropdown → verify previously opened files listed
7. Pinch in/out on trackpad → verify zoom changes and toolbar updates in sync
8. Production build: right-click PDF → Open With Papercut → file loads in editor
9. Click sign/redact in sidebar → verify clear access path to those tools
  </how-to-verify>
  <resume-signal>Type "approved" or describe any remaining issues</resume-signal>
</task>

</tasks>

<verification>
- All 5 new features are functional
- No regressions from 16-08 bug fixes
- Editor feels more complete and intuitive per user feedback
</verification>

<success_criteria>
All 5 user-requested features are implemented and verified during Task 6 checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/16-pdf-editor-gs-bundling/16-09-SUMMARY.md`
</output>
