// Auto-updater is disabled until signing keys are configured.
// Re-enable by adding tauri-plugin-updater back and setting a valid pubkey in tauri.conf.json.
export function UpdateChecker() {
  return null;
}
